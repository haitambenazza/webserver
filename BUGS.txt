1- we might need to refactor how the multiplexer handle the server and client and how the server read from the buffer

2- in parser when you pass server as server name it crashes

server
{
    listen 8000;
    host 127.0.0.1;
    index index.html;
    server_name server2;
    root /var/www/example.com/html;
    location / {
        index index.html;
    }
}

3- the server crashes with s.GetRoot() in method.c

1- we might need to refactor how the multiplexer handle the server and client and how the server read from the buffer


2- the next test will create 2 servers which result in memroy leaks
server
{
    listen 8000;
    host 127.0.0.1;
    index index.html;
    server server2;
    root /var/www/example.com/html;
    location / {
        index index.html;
    }
}

3- we need a Client class to store Fd of clients if the server quite while the clients are connected (fd leaks)

4- if a client connected to the server and the server exit, the Fd of the client doesnt get close because the struct is empty


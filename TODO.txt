1- parsing config file
	- exit gracfully when the config file is misconfigured
	- getaddrinfo to parse the IP:PORT
	- create a map of HTTP errors (404)
	- specify the location for POST method
	- specify redirection key

2- establish the server with multiplexing and Non-blocking I/O (concurrency)
	=> for multiplexer
		. to close the client we can set a timeout or when epoll returns an error or the data has been served (GET => POST)
